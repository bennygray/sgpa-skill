# TDD 标准模板（Technical Design Document）

> **适用**：Phase D+ 新功能的技术设计文档
> **产出者**：/SGA
> **保存路径**：`docs/design/specs/[name]-TDD.md`
> **前置条件**：对应 PRD.md 有 GATE 1 签章

---

## §1 全局对齐

### 1.1 分层归属

| 层 | 涉及文件 | 说明 |
|----|---------|------|
| Data Layer (`shared/`) | [新增/修改的文件] | [说明] |
| Engine Layer (`engine/`) | [新增/修改的文件] | [说明] |
| AI Layer (`ai/`) | [如适用] | [说明] |
| Presentation Layer (`ui/`) | [如适用] | [说明] |

### 1.2 Invariant 验证结果

| PRD Invariant | 架构兼容性 | 说明 |
|--------------|:----------:|------|
| I1: [不变量] | ✅/⚠️ | [分析] |

---

## §2 Interface 草案

```typescript
// 新增 / 修改的 interface
// 注明每个字段的 Owner（写入者）
```

---

## §3 GameState 变更

### 3.1 新增字段

| 字段路径 | 类型 | 默认值 | Owner | Reader |
|---------|------|--------|-------|--------|
| `state.xxx` | `Type` | `default` | [系统] | [系统列表] |

### 3.2 createDefaultLiteGameState 更新

[需要添加的默认值说明]

---

## §4 存档迁移

### 4.1 Schema 变更

| 版本 | 变更 |
|:----:|------|
| vN → vN+1 | +field1, +field2, -field3 |

### 4.2 迁移函数

```typescript
// migrateVNtoVN+1 函数签名和关键逻辑
```

---

## §5 Pipeline 挂载

### 5.1 挂载方案

| Handler 名称 | 阶段（Phase） | 优先级 | 说明 |
|-------------|:-------:|:------:|------|
| [handler-name] | [100-700] | [步进] | [逻辑说明] |

### 5.2 执行顺序验证

[确认与现有 Handler 的顺序关系无冲突]

---

## §6 依赖矩阵变更

### 6.1 新增依赖

| 新文件 | 依赖 | 类型 |
|--------|------|:----:|
| [file] | [被依赖文件] | R/W |

### 6.2 影响评估

[现有哪些系统会被影响？影响方式是什么？]

---

## §7 回归影响清单

| 已有系统 | 影响方式 | 回归验证方式 |
|---------|---------|------------|
| [系统名] | [影响描述] | [验证命令/脚本] |

---

## §8 ADR 记录

[使用 `_shared/templates/adr-template.md` 格式，如需要]

---

## §9 Party Review 报告

| # | 角色 | 维度 | 判定 | 说明 | CoVe |
|---|------|------|:----:|------|------|
| 1 | R4 | [维度] | ✅/⚠️/🔴 | [说明] | [结果] |

**最终判定**：✅ PASS / ⚠️ CONDITIONAL / 🔴 BLOCKED

---

## §10 SGA Signoff

- [ ] Interface 设计完整
- [ ] 存档迁移策略完整
- [ ] Pipeline 挂载方案确认
- [ ] 依赖矩阵已更新
- [ ] Party Review 无 BLOCK 项

签章：`[ ] GATE 2 PASSED` — [日期]

---

## 变更日志

| 日期 | 级别 | 变更内容 | 影响范围 | 回退到 | 批准人 |
|------|------|----------|----------|--------|--------|
| — | — | — | — | — | — |
