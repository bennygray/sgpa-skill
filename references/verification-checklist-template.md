# 集成验证检查清单模板

> 每一行验证必须严格按照 Verification Gate 五步执行：
> **IDENTIFY**（确定命令）→ **RUN**（执行）→ **READ**（读输出）→ **VERIFY**（确认通过？）→ **CLAIM**（宣称结果）
> 禁止使用 "应该通过"、"大概没问题" 等模糊词。

## [系统名称] 验证清单

### 一、功能验证（基于 Step 6 的 AC）

| Story# | AC# | 验证方式 | 命令/操作 | 预期结果 | 实际结果 | 状态 |
|--------|-----|---------|----------|---------|---------|------|
| 1 | 1 | 手动/自动 | `具体命令` | 具体预期 | — | ⬜ |

### 二、数值验证（基于 Step 8 的脚本）

| 验证脚本 | 运行命令 | 通过标准 | 实际输出 | 状态 |
|---------|---------|---------|---------|------|
| `scripts/xxx-sim.ts` | `npx tsx scripts/xxx-sim.ts` | [具体阈值] | — | ⬜ |

### 三、性能验证（基于 AGENTS.md §3.1）

| 指标 | 红线 | 测量方式 | 实际值 | 状态 |
|------|------|---------|-------|------|
| CPU 占用（挂机） | ≤ 3% | 任务管理器 / DevTools | — | ⬜ |
| 内存占用 | ≤ 300MB | 任务管理器 | — | ⬜ |
| IPC 调用频率 | < 15 次/秒 | 计数器日志 | — | ⬜ |

### 四、回归验证

> 以下为示例条目。实际执行 Step 9 时，应基于当前系统与其他系统的
> 挂载关系（Step 1 循环挂载 + Step 3 产源消耗）动态列出所需的回归验证脚本。

| 已有系统 | 验证方式 | 状态 |
|---------|---------|------|
| （示例）打坐收益 | `npx tsx scripts/headless-sim.ts` | ⬜ |
| （示例）炼丹系统 | `npx tsx scripts/alchemy-sim.ts` | ⬜ |
| ... 根据实际挂载关系补充 | ... | ⬜ |

### 验证门禁规则

完成以上全部 ⬜ → ✅ 后，方可执行 Step 10 交接。
任何一项为 ❌，必须修复后重新验证。
